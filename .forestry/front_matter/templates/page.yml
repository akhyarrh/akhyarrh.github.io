---
label: Page
hide_body: false
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: teaser_image
  type: file
  config:
    maxSize: 64
  label: Teaser Image
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
