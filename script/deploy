# Deploy compiled site to master branch
# because this is user page
#
#!/bin/bash

set -e

cd _site

git config user.email "build@akhyarrh.github.io"
git config user.name "Travis CI"

echo "[View live](https://${GH_USER}.github.io)" > README.md

# add, commit and push files

git add --all
git commit -m "Travis build $TRAVIS_BUILD_NUMBER from commit $TRAVIS_COMMIT"
git push origin master > /dev/null

echo "Done deploying to master"

# Use this script to submit sitemaps
# automatically to Google & Bing

curl -I "http://www.bing.com/webmaster/ping.aspx?siteMap=https://akhyarrh.github.io/sitemap.xml"

curl -I "http://www.google.com/webmasters/sitemaps/ping?sitemap=https://akhyarrh.github.io/sitemap.xml"

echo "Done ping search engine"
